#include <stdio.h>

/**
 * main - Print all digits of base 10
 * Return: always return 0
 */

int main(void)
{

	int digit;

	for (digit = 48; digit <= 57; digit++)
	{
		putchar(digit);
	}

	putchar('\n');

	return (0);
}
