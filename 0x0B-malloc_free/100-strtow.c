#include "holberton.h"
#include <stdlib.h>
#include <stddef.h>

/**
 * strtow - splits a string into words
 * @str: targeted string
 * Return: return
 */

char **strtow(char *str)
{
	char **words;

	words = '\0';

	if (str == NULL)
	{
		return (NULL);
	}

	return (words);
}
